// import React, { createContext, useState, ReactNode } from "react";

// interface AppContextProps {
//   children: ReactNode;
// }

// interface AppContextValue {
//   // Define the structure of your context value here
// }

// const AppContext = createContext<AppContextValue | undefined>(undefined);

// export function AppProvider({ children }: AppContextProps) {
//   // Your state and other logic here

//   const contextValue: AppContextValue = {
//     // Initialize your context value here
//   };

//   return (
//     <AppContext.Provider value={contextValue}>
//       {children}
//     </AppContext.Provider>
//   );
// }

// export { AppContext }; // Export AppContext for use in other files
